# Boardroom dashboard — server-side only. Set these in the Vercel project's
# Environment Variables (NOT NEXT_PUBLIC_*, so they never reach the browser).
SUPABASE_URL=https://qyaekaifodgiaxyztpdt.supabase.co
SUPABASE_SERVICE_KEY=
